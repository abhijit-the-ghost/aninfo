# Aninfo Environment Variables

# API Configuration
# The Jikan API is free and doesn't require an API key
# Base URL is already configured in src/api/client.ts
VITE_API_BASE_URL=https://api.jikan.moe/v4

# Deployment
# These are automatically handled by vite.config.ts
# Development: VITE_BASE_URL=/
# Production: VITE_BASE_URL=/aninfo/
